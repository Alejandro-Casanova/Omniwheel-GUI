import PropTypes from 'prop-types'

const terminalMessage_types = {
  content: PropTypes.node,
  style: PropTypes.object,
  className: PropTypes.string,
  dangerMode: PropTypes.bool
}

export default terminalMessage_types;